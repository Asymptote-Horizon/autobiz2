# Place your prediction model file (prediction_model.pkl) in this directory.
# The backend will automatically load and use it for the /api/predict endpoint.
# If no model is present, demo predictions will be generated instead.
